#include "main.h"
#include <stdio.h>
/**
 * mul - function that multiplies
 *@a: first int
 * @b: second int
 *
 * Return: returns result
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
