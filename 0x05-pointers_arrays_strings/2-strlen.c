#include "main.h"
#include <string.h>
/**
 * _strlen - returns length of a string
 * @s: string we wanna count
 *
 * Return: returns length
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
