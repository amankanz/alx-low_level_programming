#include "main.h"
/**
 * add - function that adds two numbers
 * @a: first integer
 * @b: second integer
 *
 * Return: return the sum
 */
int add(int a, int b)
{
	int sum;

	sum = (a + b);
	return (sum);
}
